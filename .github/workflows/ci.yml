name: Swift
on: [push]
jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build
        run: set -o pipefail && xcodebuild clean build CODE_SIGNING_ALLOWED=NO
